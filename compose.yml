services:

  django-db:
    image: "docker.io/library/postgres:17-alpine3.22"
    container_name: "django-db"
    hostname: "django-db"
    environment:
      POSTGRES_USER: "${APP_USER}"
      POSTGRES_PASSWORD: "${APP_USER}"
      POSTGRES_HOST_AUTH_METHOD: md5
      DB_NAME: "${DB_NAME}"
    volumes:
      - ./files/sql:/sql
      - ./files/data/output:/opt
      - ./files/sql/api.schema:/opt/api.schema
      - ./files/data/fastafiles.md5:/fastafiles.md5
      - ./files/backups/django.pgc:/django.pgc
      - ./files/backups/django_training.pgc:/django_training.pgc
      - ./files/scripts/export.sh:/export.sh
      - ./files/scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
      - ./files/scripts/init-env.sh:/init-env.sh
networks:
  default:
    name: django